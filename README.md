# olimpiadas
## Instalación
```python
#Instalación de libs
$ pip install -r requirements.txt

#Correr el servidor en heroku
#Settings.py ya configurado
$ py manage.py runserver

#Correr el servidor en local
#Es necesario modificar el settings.py, cambiando el DEBUG = FALSE. También comentar (#) la linea 13 y 131
$ py manage.py runserver
```
